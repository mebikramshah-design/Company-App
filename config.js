// Zero-config demo: runs entirely in the browser using localStorage.
// Set DEMO_MODE = false and provide API_BASE to use a real backend.
window.DEMO_MODE = true;
window.API_BASE = window.API_BASE || 'http://localhost:5000/api';
